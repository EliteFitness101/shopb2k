import { createFileRoute, Link, Outlet, useRouterState } from "@tanstack/react-router";
import { useEffect, useState, type FormEvent } from "react";
import { ArrowRight, LocateFixed, Search } from "lucide-react";
import { SiteFooter } from "@/components/SiteFooter";
import { SiteHeader } from "@/components/SiteHeader";
import { trackEvent } from "@/lib/revenueOS";
import heroImg from "@/assets/hero-barbell.jpg";

const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL as string | undefined;
const SUPABASE_KEY = import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY as string | undefined;
const WELLNESS_HERO_VIDEO = "https://ab2ttlkn9no0tuoa.public.blob.vercel-storage.com/buffer/assets/wellness/bg-wellness.mp4";
const WELLNESS_HERO_POSTER = "https://ab2ttlkn9no0tuoa.public.blob.vercel-storage.com/buffer/assets/cover/resofit-cover-3.jpeg";
type State = { id:string; state_code:string; name:string; slug:string; status:string };
async function locator<T>(params:Record<string,string|number|undefined>){if(!SUPABASE_URL)throw new Error("Wellness locator is not configured");const q=new URLSearchParams();Object.entries(params).forEach(([k,v])=>{if(v!==undefined&&v!=="")q.set(k,String(v))});const r=await fetch(`${SUPABASE_URL}/functions/v1/wellness-locator?${q}`,{headers:SUPABASE_KEY?{apikey:SUPABASE_KEY}:undefined});if(!r.ok)throw new Error(`Wellness locator failed: ${r.status}`);return r.json() as Promise<T>}
export const Route=createFileRoute("/wellness")({head:()=>({meta:[{title:"Wellness — Find Your Wellness Path | ResoFit"},{name:"description",content:"Discover wellness services, locations and wellness hubs across Nigeria through ResoFit."},{property:"og:url",content:"https://resofit.fit/wellness"}],links:[{rel:"canonical",href:"https://resofit.fit/wellness"}]}),component:WellnessRoute});
function WellnessRoute(){const isHome=useRouterState({select:s=>s.location.pathname==="/wellness"});return isHome?<WellnessHome/>:<Outlet/>}
function WellnessHome(){const[states,setStates]=useState<State[]>([]);const[query,setQuery]=useState("");const[locationMessage,setLocationMessage]=useState("");const[loading,setLoading]=useState(false);useEffect(()=>{trackEvent("wellness_view");locator<{states:State[]}>({action:"states"}).then(d=>setStates(d.states.filter(s=>s.status==="active"))).catch(()=>setLocationMessage("Wellness discovery is temporarily unavailable."))},[]);const locate=()=>{if(!navigator.geolocation){setLocationMessage("Location services are unavailable. Choose a state instead.");return}setLoading(true);setLocationMessage("Resolving your location…");navigator.geolocation.getCurrentPosition(({coords})=>{trackEvent("wellness_location_detected");window.location.href=`/wellness/states/cities/hubs/geo-locator?lat=${coords.latitude}&lng=${coords.longitude}`},()=>{setLoading(false);setLocationMessage("Location permission was not granted. Choose a state instead.")},{enableHighAccuracy:true,timeout:10000,maximumAge:300000})};const submitSearch=(e:FormEvent)=>{e.preventDefault();const value=query.trim();if(!value)return;trackEvent("wellness_search");window.location.href=`/wellness/states/cities/hubs/geo-locator?q=${encodeURIComponent(value)}`};return <div className="min-h-screen bg-background"><SiteHeader/><main><section className="relative min-h-[72svh] overflow-hidden border-b border-border/60"><div className="absolute inset-0 bg-background"><img src={WELLNESS_HERO_POSTER} alt="" aria-hidden="true" width={1200} height={630} fetchPriority="high" className="absolute inset-0 h-full w-full object-cover opacity-60" onError={e=>{e.currentTarget.src=heroImg}}/><video className="absolute inset-0 h-full w-full object-cover opacity-70" src={WELLNESS_HERO_VIDEO} autoPlay muted loop playsInline preload="metadata" poster={WELLNESS_HERO_POSTER} aria-hidden="true" onError={e=>{e.currentTarget.style.display="none"}}/><div className="absolute inset-0 bg-gradient-to-r from-background via-background/75 to-background/15"/><div className="absolute inset-0 bg-gradient-to-t from-background via-transparent to-background/35"/></div><div className="relative z-10 mx-auto max-w-7xl px-6 py-24 md:py-32"><p className="mb-5 text-xs uppercase tracking-[0.35em] text-gold">ResoFit™ Wellness Network</p><h1 className="font-display text-6xl leading-[0.92] md:text-8xl">What wellness<br/><span className="text-gradient-gold">do you need today?</span></h1><p className="mt-7 max-w-2xl text-lg leading-relaxed text-muted-foreground">Tell us what you need, where you are, or simply use your location.</p><form onSubmit={submitSearch} className="mt-10 max-w-3xl"><div className="flex items-center gap-3 border border-border bg-card/40 p-2"><Search className="ml-3 h-5 w-5"/><input aria-label="Search wellness" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search for a service, location, need or wellness goal…" className="min-w-0 flex-1 bg-transparent px-2 py-4 outline-none"/><button className="inline-flex h-12 items-center gap-2 bg-gold px-5 text-xs font-semibold uppercase tracking-widest text-gold-foreground">Search<ArrowRight className="h-4 w-4"/></button></div></form><div className="mt-4 flex flex-wrap items-center gap-4"><button type="button" onClick={locate} disabled={loading} className="inline-flex items-center gap-2 border border-border px-4 py-3 text-xs font-semibold uppercase tracking-widest"><LocateFixed className="h-4 w-4"/>{loading?"Locating…":"Use my location"}</button>{locationMessage&&<span className="text-sm text-muted-foreground">{locationMessage}</span>}</div></div></section><section className="border-b border-border/60 py-20"><div className="mx-auto max-w-7xl px-6"><div className="mb-8 flex items-end justify-between"><div><p className="mb-3 text-xs uppercase tracking-[0.3em] text-gold">Explore Nigeria</p><h2 className="font-display text-4xl md:text-5xl">Find wellness by location.</h2></div><Link to="/wellness/states/cities/hubs/geo-locator" className="text-xs uppercase tracking-widest text-muted-foreground">Open locator →</Link></div><div className="grid grid-cols-2 gap-px bg-border/60 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5">{states.map(s=><a key={s.id} href={`/wellness/states/cities/hubs/geo-locator?state=${encodeURIComponent(s.slug)}`} className="bg-background p-5 text-sm hover:bg-card hover:text-gold"><span className="block text-[10px] uppercase tracking-[0.2em] text-muted-foreground">{s.state_code}</span><span className="mt-2 block font-medium">{s.name}</span></a>)}</div></div></section></main><SiteFooter/></div>}
