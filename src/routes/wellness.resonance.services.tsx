import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, Check } from "lucide-react";
import { SiteFooter } from "@/components/SiteFooter";
import { SiteHeader } from "@/components/SiteHeader";
import { MAKAVELI_CATEGORIES, MAKAVELI_SERVICES, formatNGN } from "@/lib/makaveliCatalog";

export const Route = createFileRoute("/wellness/resonance/services")({
  head:()=>({meta:[{title:"Resonance Fitness Wellness Services — ResoFit"},{name:"description",content:"Resonance Fitness wellness services, pricing, booking and secure checkout."}],links:[{rel:"canonical",href:"https://resofit.fit/wellness/resonance/services"}]}),
  component: ResonanceServices,
});
function ResonanceServices(){
 return <div className="min-h-screen bg-background"><SiteHeader/><main><section className="border-b border-border/60 bg-gradient-to-b from-gold/[0.06] to-transparent"><div className="mx-auto max-w-7xl px-6 py-20 md:py-28"><p className="text-xs uppercase tracking-[.35em] text-gold">Resonance Fitness · Umuahia</p><h1 className="mt-4 max-w-5xl font-display text-5xl leading-[.95] md:text-8xl">Wellness services.<br/><span className="text-gradient-gold">Ready to book.</span></h1><p className="mt-7 max-w-2xl text-lg leading-relaxed text-muted-foreground">Review the complete published service menu. Fixed-price services can be booked and paid online; request-only services remain enquiry-led.</p><div className="mt-8"><Link to="/wellness/resonance/book" className="inline-flex items-center gap-2 bg-gold px-6 py-3 text-xs font-semibold uppercase tracking-widest text-gold-foreground">Start booking <ArrowRight className="h-4 w-4"/></Link></div></div></section><section className="py-12"><div className="mx-auto max-w-7xl px-6">{MAKAVELI_CATEGORIES.map(category=>{const services=MAKAVELI_SERVICES.filter(s=>s.category===category);const id=category.toLowerCase().replace(/[^a-z0-9]+/g,"-");return <section key={category} id={id} className="scroll-mt-24 py-10"><div className="mb-6 flex items-end justify-between"><h2 className="font-display text-3xl md:text-5xl">{category}</h2><span className="text-xs text-muted-foreground">{services.length} options</span></div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{services.map(service=>{const canPay=service.booking==="pay"&&service.status==="available";const price=service.priceMode==="fixed"&&service.price?formatNGN(service.price):service.priceLabel??(service.priceMode==="referral"?"Referral":"Custom quote");return <article key={service.slug} className="flex min-h-[390px] flex-col rounded-2xl border border-border/70 bg-card/30 p-6"><div className="flex justify-between gap-3"><span className="text-[10px] uppercase tracking-widest text-muted-foreground">{canPay?"Book & pay":"Request"}</span><span className="font-display text-xl text-gold">{price}</span></div><h3 className="mt-5 font-display text-2xl">{service.name}</h3><p className="mt-3 text-sm leading-relaxed text-muted-foreground">{service.description}</p><ul className="mt-5 space-y-2">{service.features.slice(0,4).map(x=><li key={x} className="flex gap-2 text-xs text-muted-foreground"><Check className="h-3.5 w-3.5 shrink-0 text-gold"/>{x}</li>)}</ul><div className="mt-auto pt-6"><Link to="/wellness/resonance/book" search={{service:service.slug}} className="inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-lg bg-gold px-4 py-3 text-[10px] font-semibold uppercase tracking-widest text-gold-foreground">{canPay?"Book & pay":"Request booking"}<ArrowRight className="h-3.5 w-3.5"/></Link><Link to="/wellness/resonance/services/$slug" params={{slug:service.slug}} className="mt-4 inline-flex text-[10px] font-semibold uppercase tracking-widest text-gold">View service details <ArrowRight className="ml-2 h-3.5 w-3.5"/></Link></div></article>})}</div></section>})}</div></section></main><SiteFooter/></div>;
}
