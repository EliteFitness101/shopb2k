import { createFileRoute } from "@tanstack/react-router";
import { MARTIAL_X_ASSETS } from "@/content/martialXAssets";
export const Route=createFileRoute("/martial-x3/rsdn")({component:RsdnPage});
function RsdnPage(){const assets=MARTIAL_X_ASSETS.filter(a=>a.type==="rsdn");return <main className="min-h-dvh bg-black text-white"><section className="relative min-h-[70vh] overflow-hidden"><img src={assets[0].url} alt={assets[0].altSeed} className="absolute inset-0 h-full w-full object-cover opacity-65"/><div className="absolute inset-0 bg-black/60"/><div className="relative mx-auto flex min-h-[70vh] max-w-6xl flex-col justify-end px-6 py-16"><p className="text-xs uppercase tracking-[.35em] text-amber-300">Martial-X · RSDN</p><h1 className="mt-3 text-5xl font-semibold md:text-7xl">RSDN Project Network</h1><p className="mt-5 max-w-2xl text-white/75">Project cover and network presentation surface for the RSDN MOUAU initiative.</p></div></section><section className="mx-auto grid max-w-6xl gap-6 px-6 py-16 md:grid-cols-2">{assets.map(a=><figure key={a.id} className="overflow-hidden rounded-2xl border border-white/10 bg-white/5"><img src={a.url} alt={a.altSeed} className="w-full object-cover"/><figcaption className="p-4 text-sm text-white/70">{a.contentRole}</figcaption></figure>)}</section></main>}
