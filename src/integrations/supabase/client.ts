// This file is automatically generated. Do not edit it directly.
import { createClient } from "@supabase/supabase-js";
import type { Database } from "./types";
function isNewSupabaseApiKey(value: string): boolean { return value.startsWith("sb_publishable_") || value.startsWith("sb_secret_"); }
function createSupabaseFetch(supabaseKey: string): typeof fetch { return (input, init) => { const headers = new Headers(typeof Request !== "undefined" && input instanceof Request ? input.headers : undefined); if (init?.headers) new Headers(init.headers).forEach((value,key)=>headers.set(key,value)); if (isNewSupabaseApiKey(supabaseKey) && headers.get("Authorization") === `Bearer ${supabaseKey}`) headers.delete("Authorization"); headers.set("apikey",supabaseKey); return fetch(input,{...init,headers}); }; }
const SHARED_AUTH_COOKIE="rf_auth_session_v1", COOKIE_CHUNK_SIZE=2800;
function sharedCookieEnabled(){return typeof window!=="undefined"&&(window.location.hostname==="resofit.fit"||window.location.hostname.endsWith(".resofit.fit"));}
function readCookie(name:string){const prefix=name+"=";const part=sharedCookieEnabled()?document.cookie.split("; ").find(e=>e.startsWith(prefix)):null;return part?decodeURIComponent(part.slice(prefix.length)):null;}
function writeCookie(name:string,value:string,maxAge=31536000){document.cookie=name+"="+encodeURIComponent(value)+"; Max-Age="+maxAge+"; Path=/; Domain=.resofit.fit; Secure; SameSite=Lax";}
function clearCookie(name:string){writeCookie(name,"",0);}
const sharedAuthStorage:Storage={getItem(key){if(!sharedCookieEnabled())return localStorage.getItem(key);const count=Number(readCookie(SHARED_AUTH_COOKIE+"_count")??"0");if(count>0){let v="";for(let i=0;i<count;i++)v+=readCookie(SHARED_AUTH_COOKIE+"_"+i)??"";if(v)return v;}const legacy=localStorage.getItem(key);if(legacy)this.setItem(key,legacy);return legacy;},setItem(key,value){if(!sharedCookieEnabled()){localStorage.setItem(key,value);return;}const count=Math.ceil(value.length/COOKIE_CHUNK_SIZE)||1;const old=Number(readCookie(SHARED_AUTH_COOKIE+"_count")??"0");for(let i=0;i<Math.max(old,count);i++)clearCookie(SHARED_AUTH_COOKIE+"_"+i);for(let i=0;i<count;i++)writeCookie(SHARED_AUTH_COOKIE+"_"+i,value.slice(i*COOKIE_CHUNK_SIZE,(i+1)*COOKIE_CHUNK_SIZE));writeCookie(SHARED_AUTH_COOKIE+"_count",String(count));localStorage.removeItem(key);},removeItem(key){if(!sharedCookieEnabled()){localStorage.removeItem(key);return;}const count=Number(readCookie(SHARED_AUTH_COOKIE+"_count")??"0");for(let i=0;i<count;i++)clearCookie(SHARED_AUTH_COOKIE+"_"+i);clearCookie(SHARED_AUTH_COOKIE+"_count");localStorage.removeItem(key);},clear(){const count=Number(readCookie(SHARED_AUTH_COOKIE+"_count")??"0");for(let i=0;i<count;i++)clearCookie(SHARED_AUTH_COOKIE+"_"+i);clearCookie(SHARED_AUTH_COOKIE+"_count");localStorage.clear();},key(i){return localStorage.key(i);},get length(){return localStorage.length;}};
function createSupabaseClient(){const SUPABASE_URL=import.meta.env.VITE_SUPABASE_URL||process.env.SUPABASE_URL;const SUPABASE_PUBLISHABLE_KEY=import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY||process.env.SUPABASE_PUBLISHABLE_KEY;if(!SUPABASE_URL||!SUPABASE_PUBLISHABLE_KEY)throw new Error("Missing Supabase environment variable(s). Connect Supabase in Lovable Cloud.");return createClient<Database>(SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY,{global:{fetch:createSupabaseFetch(SUPABASE_PUBLISHABLE_KEY)},auth:{storage:typeof window!=="undefined"?sharedAuthStorage:undefined,persistSession:true,autoRefreshToken:true}});}
let _supabase:ReturnType<typeof createSupabaseClient>|undefined;
export const supabase=new Proxy({} as ReturnType<typeof createSupabaseClient>,{get(_,prop,receiver){if(!_supabase)_supabase=createSupabaseClient();return Reflect.get(_supabase,prop,receiver);}});
