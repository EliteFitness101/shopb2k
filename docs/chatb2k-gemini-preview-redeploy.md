# ChatB2K Gemini Preview Runtime

This additive marker intentionally triggers a fresh Preview deployment after the production/preview `GEMINI_API_KEY` environment variable was configured in Vercel `reso-web`.

No production route, table, API, publisher, Buffer channel, or campaign is replaced or removed.
